@model IEnumerable<EPSN.Gallery_Video>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Video)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Video)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Video }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Video }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Video })
        </td>
    </tr>
}

</table>
